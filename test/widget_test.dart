// Widget smoke test — substitui o template padrão do Flutter.
// Testes funcionais reais estão em test/converter_provider_test.dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // Testes de widget requerem AdMob inicializado — ver converter_provider_test.dart
    expect(true, isTrue);
  });
}
